<Page x:Class="Sup20_12.View.HighScorePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Sup20_12.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" 
      Title="HighScorePage">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ResourceDictionaries/Style.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/Images/bgHighscorePage.jpg" Stretch="UniformToFill"/>
        </Grid.Background>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="45"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="150px"/>
                <RowDefinition Height="105px"/>
                <RowDefinition Height="55px"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="34"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="3" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="40px"/>
                </Grid.RowDefinitions>
                <Button Style="{StaticResource BtnBack}" Grid.Column="0" Grid.Row="1" Command="{Binding GoToMainPage}" Background="{x:Null}" BorderBrush="{x:Null}" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
            </Grid>


            <TextBox Text="{Binding HighScoreLst}" Grid.Column="2" Grid.Row="1" FontSize="16" BorderThickness="1" ScrollViewer.VerticalScrollBarVisibility="auto" />
            <TextBox Text="{Binding FrequentPlayers}" Grid.Column="2" Grid.Row="3" FontSize="16" BorderThickness="1" ScrollViewer.VerticalScrollBarVisibility="Disabled" />



        </Grid>
    </Grid>
</Page>
